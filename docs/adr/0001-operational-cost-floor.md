# Operational cost uses floor semantics

Operational cost per service-period is modeled as the greater of variable usage cost and minimum capacity-step cost, not as fixed cost plus variable cost. This preserves the methodology where operational capacity has a minimum economic floor and variable usage only dominates once it exceeds that floor.
